.env
.env.local
# Operator-local compose override (e.g. wallet mount); must survive autoupdate's
# `git reset --hard`, so keep it untracked.
docker-compose.override.yml
*.log
.DS_Store
.venv/
__pycache__/
*.pyc
.ruff_cache/
.ty_cache/
*.egg-info/
